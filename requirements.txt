# Runtime: numpy + a TPU VM with libtpu installed (no torch / torch_xla / jax).
numpy>=1.24
libtpu>=0.0.17        # the PJRT plugin (pip install libtpu, or use a TPU VM image)
requests             # examples/ only: downloads the tiny-shakespeare dataset
